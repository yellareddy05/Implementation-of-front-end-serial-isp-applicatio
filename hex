#include<stdio.h>
#include<string.h>
main()
{
        char hex[20]={0};
        int i;
        printf("enter the hex file name\n");
        scanf("%s",hex);
        puts(hex);
        for(i=0;hex[i];i++)
        {
                if(hex[i]>=65 && hex[i]<=90)
                        hex[i]+=32;
        }
        printf("after conversation\n");
        puts(hex);
}

